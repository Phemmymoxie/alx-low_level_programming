#include "main.h"

/**
 * set_string - A function that sets a string to another
 * @s: the pointer to a string to be set
 * @to: the pointer argument to be set to
 * Return: Always 0.
 */
void set_string(char **s, char *to)
{
		*s = to;
}
