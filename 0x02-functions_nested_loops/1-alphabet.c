#include "main.h"

/**
 * print_alphabet - print out all alphabets in lowercase.
 *
 * Return: Always 0.
 */

void print_alphabet(void)
{

	int g;

	for (g = 'a'; g <= 'z'; g++)
	{
	_putchar(g);
	}
	_putchar('\n');
}
