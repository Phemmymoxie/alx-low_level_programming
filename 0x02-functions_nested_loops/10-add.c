#include <stdio.h>
#include "main.h"

/**
 * add - code that add two integers and return the result.
 * @q: the first argument
 * @r: the second argument
 * Return: Always 0.
 */

int add(int q, int r)
{
	int addup = q + r;

	return (addup);
}
